using System.Collections;
using System.Collections.Generic;
using Unity.XR.CoreUtils;
using UnityEngine;

public class grabFix : MonoBehaviour
{
    public Collider test;
    // Start is called before the first frame update
    void Start()
    {
        //var characterController = FindObjectOfType<XROrigin>();
        //var list = characterController.GetComponentsInChildren<Collider>();
        //if (characterController != null) {
        //    foreach (var collider in list) {
        //        Physics.IgnoreCollision(collider, test, true);
        //    }
        //}
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
